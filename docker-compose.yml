services:
  app:
    build: .
    ports:
      - "${HOST_PORT:-7432}:7432"
    volumes:
      - dnd_data:/data
    environment:
      - STORAGE_PATH=/data
    restart: unless-stopped

volumes:
  dnd_data:
